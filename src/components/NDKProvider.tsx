// Re-export from new location for backwards compatibility
// TODO: Update imports to use @lib/ndk directly and remove this file
export { NDKContext } from '../lib/ndk/NDKProvider';
export { default } from '../lib/ndk/NDKProvider';
