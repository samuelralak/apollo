// Re-export from new location for backwards compatibility
// TODO: Update imports to use @/app directly and remove this file
export { configureAppStore, type RootState, type AppDispatch, type PreloadedState } from '../app/store';
